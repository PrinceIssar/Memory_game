const cards = document.querySelectorAll('.memory-card');let hasFlippedCard = false;let lockBoard = false;let firstCard , secondCard;function flipCard(){ // declare flipCard function    if (lockBoard) return;    if (this === firstCard) return; this.classList.toggle('flip'); // toggle means if class is there remove it , if it's not then add it.    if (!hasFlippedCard) {        //first click        hasFlippedCard = true;        firstCard = this;        return;    }    //second card click    secondCard = this;    checkForMatch();}function checkForMatch(){    let isMatch =firstCard.dataset.framework ===        secondCard.dataset.framework;    isMatch ? disableCards() : unFlipCards(); // itternary operator is better then if else statement , it has 3 blocks :- condition expr1 true  : expr2 false}function disableCards(){    // it's a match    firstCard.removeEventListener('click', flipCard);    secondCard.removeEventListener('click', flipCard);    resetBoard();}function unFlipCards(){    lockBoard = true;    setTimeout(() => { // setTimeOut to see at least whats behind the image        firstCard.classList.remove('flip');        secondCard.classList.remove('flip');        resetBoard();    }, 1300);// how long it will flip}function resetBoard(){ // ES6 destructuring assigment    [hasFlippedCard, lockBoard] = [false, false];    [firstCard, secondCard] = [null, null];}(function shuffle(){    cards.forEach(card =>{        let randomPos = Math.floor(Math.random()*12);        card.style.order = randomPos;    });})();cards.forEach(card => card.addEventListener('click', flipCard)); // each card we attach eventListener click and then function execute called flipCard